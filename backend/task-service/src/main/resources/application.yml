spring:
  application:
    name: task-service
  datasource:
    url: jdbc:mysql://localhost:3306/ai_lingdaima_task?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  redis:
    host: localhost
    port: 6379

mybatis-flex:
  mapper-locations: classpath*:/mappers/*.xml

dubbo:
  application:
    name: task-service
  registry:
    address: nacos://127.0.0.1:8848
  protocol:
    name: tri
    port: -1

nacos:
  discovery:
    username: nacos
    password: nacos
